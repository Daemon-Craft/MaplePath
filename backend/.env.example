# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=MaplePath API

# Database Configuration
# Local development
DATABASE_URL=postgresql://user:password@localhost:5432/maplepath_db

# Production - Cloud SQL with Unix socket (for Cloud Run)
# DATABASE_URL=postgresql://user:password@/dbname?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
DATABASE_URL_PROD=postgresql://user:password@/maplepath_prod?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME

# JWT Configuration
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Firebase Configuration (for Google Authentication)
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json

# Google Cloud / Vertex AI Configuration
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1
VERTEX_AI_MODEL=gemini-1.5-flash

# Cloud Storage (for PDF storage - optional)
GCS_BUCKET_NAME=maplepath-cvs
